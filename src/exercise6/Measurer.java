package exercise6;

public interface Measurer {
    double measure(Object obj);
}

/*
Add a method to the Data class that returns the object with the largest measure, as
measured by the supplied measurer:
public static Object largest(Object[] objects, Measurer m)

 */